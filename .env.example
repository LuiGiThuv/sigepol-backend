# =====================================================
# CONFIGURACIÓN DE SIGEPOL
# =====================================================

# Django
DEBUG=True
SECRET_KEY=tu-clave-secreta-super-segura-aqui-cambia-en-produccion
ALLOWED_HOSTS=localhost,127.0.0.1,*.heroku.com

# Database - SQLite (Desarrollo)
# USE_POSTGRES=False

# Database - PostgreSQL (Producción)
USE_POSTGRES=True
DB_ENGINE=django.db.backends.postgresql
DB_NAME=sigepol_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Email (Opcional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173

# Frontend URL
FRONTEND_URL=http://localhost:5173
